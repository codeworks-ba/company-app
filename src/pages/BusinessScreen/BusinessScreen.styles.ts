import { makeStyles } from '../../style/theme/MakeStyles';

export const styles = makeStyles({
  mainContainer: {
    padding: '0px 180px'
  },
  flexColumn: {
    display: 'flex',
    flexDirection: 'column'
  },
  flexRow: {
    display: 'flex',
    flexDirection: 'row'
  }
});
